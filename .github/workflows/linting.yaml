name: Code linting

on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development
  workflow_dispatch: {}

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v5

    - name: Setup uv
      uses: astral-sh/setup-uv@v7
      with:
        python-version: 3.11
        enable-cache: true

    - name: Install dependencies
      run: |
        # ruff is in the dev dependencies
        uv sync --dev --locked

    - name: Ruff check
      run: uv run ruff check .

    - name: Ruff format
      run: uv run ruff format .
